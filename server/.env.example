# Firebase Configuration
FIREBASE_SERVICE_ACCOUNT=./serviceAccountKey.json
FIREBASE_DATABASE_URL=https://<your-project>.firebaseio.com

# Optional: storage bucket
FIREBASE_STORAGE_BUCKET=<your-project>.appspot.com

# Server Configuration
PORT=4000
NODE_ENV=development

# Client origin (used for CORS). Set to your deployed frontend URL(s)
# or use CLIENT_URLS (comma-separated) to allow multiple origins.
CLIENT_URL=http://localhost:3000
# CLIENT_URLS=http://localhost:3000,https://grapts.vercel.app

# JWT Secret (for token signing)
JWT_SECRET=your-super-secret-key-change-this-in-production

# Optional AI gateway configuration (if using an external AI service)
# AI_GATEWAY_URL=https://your-ai-gateway.example.com/v1/search
# AI_GATEWAY_API_KEY=replace-with-your-key
